# Content

- Blog content targets a data engineering audience, with a focus on SQL and pipeline work. Confidence: 0.7
- Prefers BigQuery as the target SQL dialect/platform for SQL content (asked to produce a BigQuery SQL version of an existing blog). Confidence: 0.7
- Values readability and visual fidelity to reference documents (e.g., provided PDF screenshots): prefers the agent to replicate that visual style — dark code blocks with syntax highlighting, labeled code sections, and colored callout boxes — rather than default plain unformatted markdown. Confidence: 0.85
- Prefers borderless tables — no visible column/row lines, with alignment achieved through spacing rather than borders. Confidence: 0.9
- Prefers code inside table cells (e.g., a cheat sheet's "How to use" column) to retain the dark background and syntax highlighting, rather than rendering as transparent/inline code. Confidence: 0.9
- When SQL/code inside narrow table cells wraps messily, prefers widening the code column and/or table layout (including full-bleed beyond card padding) and flattening indentation to a shallow 2-space/max-2-level style, rather than accepting cramped deep nesting. Confidence: 0.8
- Prioritizes code width over explanation-column width in tables: prefers shrinking the "Explained"/description column so the code column gets more room, accepting that the explanation text runs longer vertically. Confidence: 0.9
- Prefers visual alignment consistency across page elements: when tables or code blocks extend full-bleed beyond the card padding, section headings and note/callout boxes should shift to align with them rather than remaining at the default padded indent. Confidence: 0.7
- In markdown tables rendered by this blog's parser, avoids backtick-escaped identifiers (`` \` ``) inside table cells — the parser treats them as inline code and shows literal backslashes, breaking the dark SQL block rendering. Uses plain table names (e.g., `analytics.orders`) in table-cell SQL and reserves backticks for fenced code blocks or prose. Confidence: 0.8
- When writing a dialect/platform-specific guide (e.g., a BigQuery cheat sheet), prefers content that is native to that platform only — not the structure of a previous SQL post with "not supported" rows for features the other dialect has. The borrowed template constrains scope, skips the platform's own functions, and includes irrelevant clauses; instead cover the platform's distinctive features comprehensively (BigQuery: ARRAY/STRUCT, native JSON, TABLESAMPLE, GROUP BY ALL, MAX_BY/MIN_BY, RANGE_BUCKET, scripts, etc.). Confidence: 0.85
- Prefers posts to end cleanly: drops redundant summary sections ("Bottom line") and stale attribution lines ("Original reference: ...") when they no longer match the content, and questions/removes sections that don't add practical value — while keeping genuinely useful parts like "Common mistakes". Confidence: 0.8
- The blog renderer only supports `- ` bullet lists, not numbered lists (`1.` items fall through into plain paragraphs) — use markdown bullets for list content. Confidence: 0.8
- The blog's renderer processes inline code (backticks) before bold, so `**` wrapping an inline-code span that contains `*` (e.g., `` **`SELECT *`** ``) fails to match and leaks literal asterisks into the page — don't bold-wrap code spans containing asterisks. Confidence: 0.8
- SQL examples in tutorial/cheat-sheet content should demonstrate the full pattern being taught — e.g., for "filter before you join", the example should filter both sides of the join via CTEs (shrink each side), not just one table. Confidence: 0.65

